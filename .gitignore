# compiled output
/dist
/node_modules
/build
/certs

# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store

# Tests
/coverage
/.nyc_output

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# dotenv environment variable files
# Tracked files (template/example files):
# - .env.example (template for all environments)
# - .env.production.example (production configuration template)
# - .env.development (development configuration - tracked for team consistency)
#
# Ignored files (contain secrets/local overrides):
.env                          # Never use this file - renamed to .env.production.example
.env.production               # Production secrets (if deployed via git)
.env.development.local        # Local development overrides
.env.test.local               # Local test overrides
.env.production.local         # Local production overrides
.env.local                    # Generic local overrides

# temp directory
.temp
.tmp

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Diagnostic reports (https://nodejs.org/api/report.html)
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

/generated/prisma
